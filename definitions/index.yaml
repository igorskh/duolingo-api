definitions:
  UserList:
    properties:
      users:
        type: array
        items:
          $ref: "./user.yaml#/definitions/User"
  SubscriptionList:
    properties:
      totalSubscriptions:
        type: integer
      subscriptions:
        type: array
        items:
          $ref: "./user.yaml#/definitions/User"
  LoginData:
    properties:
      distinctId:
        type: string
      identifier:
        type: string
      password:
        type: string
      landingUrl:
        type: string
      lastReferrer:
        type: string
